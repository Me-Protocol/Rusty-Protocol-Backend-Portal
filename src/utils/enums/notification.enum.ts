// Notification enum

export enum NotificationType {
  NEWS = 'news',
  OFFER = 'offer',
  BRAND = 'brand',
  EXPIRING = 'expiring',
  POINT = 'point',
  ORDER = 'order',
  OTHER = 'other',
}

export enum NotificationFormat {
  PUSH = 'push',
  EMAIL = 'email',
  ANY = 'any',
  NONE = 'none',
}
