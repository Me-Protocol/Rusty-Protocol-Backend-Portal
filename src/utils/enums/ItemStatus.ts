export enum ItemStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
  PRIVATE = 'private',
  PAUSED = 'paused',
  PENDING = 'pending',
  ACTIVE = 'active',
  EXPIRED = 'expired',
  COMPLETED = 'completed',
}

export enum ProductStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
  PAUSED = 'paused',
  EXPIRED = 'expired',
}
